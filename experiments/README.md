# Experiments

This directory stores all training runs and logs.

As per the project rules, every distinct hyperparameter configuration gets its own numbered folder (e.g., `exp_001_baseline`). Each experiment folder will contain:
- `config.json`
- `training_log.csv`

Never overwrite a previous experiment! Create a new folder for each distinct run.
